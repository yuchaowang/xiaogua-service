<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaogua.comments.dal.mapper.UserTypeFileMapper">
    <select id="selectByUserCodeAndUserType" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from t_user_type_file
        where user_code = #{userCode}
        and type = #{userType}
    </select>
    <delete id="deleteByUserCodeAndUserType">
        delete from t_user_type_file
        where user_code = #{userCode}
        and type = #{userType}
    </delete>
</mapper>