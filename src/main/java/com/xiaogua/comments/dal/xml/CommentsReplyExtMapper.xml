<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaogua.comments.dal.mapper.CommentsReplyMapper">
    <select id="count" resultType="java.lang.Integer">
        select count(1)
        from t_comments_reply
        where 1 = 1
        <if test="!showDeleted">
            and deleted = 0
        </if>
        <if test="commentsCode != null">
            and comments_code = #{commentsCode}
        </if>
        <if test="startDate != null and endDate != null">
            and create_date between #{startDate} and #{endDate}
        </if>
    </select>
    <select id="findPage" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from t_comments_reply
        where 1 = 1
        <if test="!showDeleted">
            and deleted = 0
        </if>
        <if test="commentsCode != null">
            and comments_code = #{commentsCode}
        </if>
        order by
        create_date
        limit #{index},#{pageSize}
    </select>
    <select id="selectByCodeWithOutDel" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from t_comments_reply
        where deleted = 0
        and code = #{code}
    </select>
</mapper>